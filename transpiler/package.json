{
    "scripts": {
        "rebuild": "npm run clean && npm run build",
        "build": "mkdir build && cd build && cmake .. && cmake --build . --config Release -DCMAKE_BUILD_TYPE=Release",
        "clean": "rm -f -q build || rd /s /q build"
    }
}